var aktuellerBildschirm = 'StartBildschirm'

function draw () {
  if (aktuellerBildschirm === 'StartBildschirm') {
    // zeichne den Startbildschirm
  } else if (aktuellerBildschirm === 'Menu') {
    // zeichne den Startbildschirm
  } else if (aktuellerBildschirm === 'Spiel') {
    // zeichne die Spielfläche
  }
}

function draw () {
  switch (aktuellerBildschirm) {
    case 'StartBildschirm':
      // zeichne den Startbildschirm
      break
    case 'Menu':
      // zeichne den Startbildschirm
      break
    default:
      // zeichne die Spielfläche
      break
  }
}
